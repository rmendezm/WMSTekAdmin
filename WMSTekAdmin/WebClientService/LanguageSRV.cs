using Binaria.WMSTek.AdminApp.Manager;

namespace Binaria.WMSTek.WebClient.Service
{
    public class LanguageSRV
    {
        private LanguageMGR languageMGR = new LanguageMGR();

        /*
        public DictionaryViewDTO LoadDictionary(string login, string password)
        {
            return languageMGR.LoadDictionary(login, password);
        }

        public DictionaryViewDTO LoadAllDictionary()
        {
            return languageMGR.LoadAllDictionarys();
        }
        */

        /*
        public LanguageViewDTO LoadLanguageList()
        {
            return languageMGR.LoadLanguageList();
        }

        public DictionaryViewDTO DisableDictionary()
        {
            return languageMGR.DisableDictionary();
        }

        public DictionaryViewDTO MaintainDictionary(DictionaryViewDTO dictionaryViewDTO)
        {
            return languageMGR.MaintainDictionary(dictionaryViewDTO);
        }

        public TranslationViewDTO LoadTranslationByContent(string content, int language)
        {
            return languageMGR.LoadTranslationByContent(content, language);
        }

        public DictionaryViewDTO DeleteUnusedDictionary()
        {
            return languageMGR.DeleteUnusedDictionary();
        }
         */ 
    }
}
